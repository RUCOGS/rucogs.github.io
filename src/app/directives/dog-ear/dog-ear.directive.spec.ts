import { DogEarDirective } from './dog-ear.directive';

describe('DogEarDirective', () => {
  it('should create an instance', () => {
    const directive = new DogEarDirective();
    expect(directive).toBeTruthy();
  });
});
