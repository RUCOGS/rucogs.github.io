import { MatButtonModifierDirective } from './mat-button-modifier.directive';

// TODO: See https://stackoverflow.com/questions/61359262/unit-testing-directive-with-arguments-in-angular-2
//       for how to implement unit tests for directives that have constructors
//       We basically have to create a test component and place the directive onto the test component.

describe('MatButtonColorDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new MatButtonModifierDirective(ElementRef);
  //   expect(directive).toBeTruthy();
  // });
});
