import { MatTabStylesDirective } from './mat-tab-styles.directive';

describe('MatTabStylesDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTabStylesDirective();
    expect(directive).toBeTruthy();
  });
});
